{
	"name": "obsidian-nerv",
	"version": "0.1.0",
	"scripts": {
		"version": "node version-bump.mjs && git add manifest.json versions.json",
		"format": "prettier --write ."
	},
	"devDependencies": {
		"prettier": "^3.8.1"
	}
}
